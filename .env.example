# Database connection string for PostgreSQL with TimescaleDB
# Format: postgresql://username:password@host:port/database
DB_CONNECTION=postgresql+psycopg://user:password@localhost:5432/funding_tracker

# Optional: Set timezone for datetime operations (defaults to UTC)
# TZ=UTC