# Database connection string for PostgreSQL with TimescaleDB
# Format: postgresql://username:password@host:port/database
DB_CONNECTION=postgresql+psycopg://postgres:postgres@timescaledb:5432/funding_tracker

# Database connection parameters for db-migrate service (shared migrations)
# These should point to the same database used by DB_CONNECTION
DB_HOST=timescaledb
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_DBNAME=funding_tracker

# Comma-separated list of exchanges to run (default: empty = run all)
# Available: aster, backpack, binance_usd-m, binance_coin-m, bybit, derive, dydx,
#            extended, hyperliquid, kucoin, lighter, okx, pacifica, paradex
# Examples:
#   EXCHANGES=hyperliquid,bybit
#   EXCHANGES=binance_usd-m
#   EXCHANGES= (empty, runs all)
EXCHANGES=

# Comma-separated list of exchanges for DEBUG logging (independent of execution)
# Examples:
#   DEBUG_EXCHANGES=hyperliquid,bybit
#   DEBUG_EXCHANGES= (empty, no debug logging)
DEBUG_EXCHANGES=

# Comma-separated list for live collection DEBUG logging (independent of general debug)
# Live collection runs every minute and is very verbose - enable only when needed
# Examples:
#   DEBUG_EXCHANGES_LIVE=paradex,hyperliquid
#   DEBUG_EXCHANGES_LIVE= (empty, no live collection debug logging)
DEBUG_EXCHANGES_LIVE=

# Number of worker instances for exchange distribution (default: 3)
# Exchanges are distributed evenly across instances using round-robin
# Examples:
#   INSTANCE_COUNT=1  # Single instance (all exchanges)
#   INSTANCE_COUNT=3  # 3 instances (default, ~5-5-4 distribution)
#   INSTANCE_COUNT=5  # 5 instances (~3 per instance)
INSTANCE_COUNT=3
